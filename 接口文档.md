# Bellego 会员管理系统接口文档

## 1. 文档说明

- 文档范围：当前后端代码中已实现的全部接口
- 接口前缀：`/api/v1`
- 鉴权方式：JWT Bearer Token
- 请求体格式：`application/json`
- 文件导入接口格式：`multipart/form-data`
- 分页返回：MyBatis-Plus `IPage`

## 2. 通用约定

### 2.1 统一响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 2.2 响应字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| code | number | 业务状态码 |
| message | string | 响应消息 |
| data | any | 响应数据 |

### 2.3 常见状态码

| 状态码 | 说明 |
|---|---|
| 200 | 操作成功 |
| 400 | 请求参数错误 |
| 401 | 未授权访问 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 2.4 鉴权请求头

```http
Authorization: Bearer {token}
```

### 2.5 分页请求参数

分页查询接口默认支持以下参数：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| pageNum | number | 否 | 1 | 页码 |
| pageSize | number | 否 | 10 | 每页数量，最大 200 |

### 2.6 分页返回示例

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [],
    "total": 0,
    "size": 10,
    "current": 1,
    "pages": 0
  }
}
```

### 2.7 常用枚举约定

#### 状态 status

| 值 | 说明 |
|---|---|
| 1 | 启用 |
| 0 | 禁用 |

#### 性别 gender

| 值 | 说明 |
|---|---|
| 1 | 男 |
| 0 | 女 |

#### 权限类型 type

| 值 | 说明 |
|---|---|
| 1 | 菜单 |
| 2 | 按钮 |

#### 会员优惠券状态 memberCoupon.status

| 值 | 说明 |
|---|---|
| 0 | 未使用 |
| 1 | 已使用 |

#### 积分规则类型 pointRule.ruleType

| 值 | 说明 |
|---|---|
| 1 | 消费积分规则 |

## 3. 认证模块

### 3.1 管理员登录

- 请求方式：`POST`
- 接口路径：`/api/v1/auth/login`
- 是否鉴权：否

#### 请求参数 `LoginDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

#### 请求示例

```json
{
  "username": "admin",
  "password": "BLG123456"
}
```

#### 返回字段 `LoginVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| token | string | JWT Token |
| adminId | string | 管理员 ID |
| username | string | 用户名 |
| realName | string | 真实姓名 |
| permissions | string[] | 权限编码列表 |

### 3.2 退出登录

- 请求方式：`POST`
- 接口路径：`/api/v1/auth/logout`
- 是否鉴权：否

> 当前实现为无状态 JWT，服务端不维护会话，前端清理 Token 即可完成退出流程。

## 4. 会员管理

### 4.1 分页查询会员

- 请求方式：`GET`
- 接口路径：`/api/v1/members`
- 权限：`member:view`

#### 查询参数 `MemberQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| keyword | string | 否 | 关键字，匹配姓名/手机号/卡号 |
| levelId | string | 否 | 会员等级 ID |
| status | number | 否 | 状态 |

#### 返回记录 `MemberVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 会员 ID |
| levelId | string | 会员等级 ID |
| cardNumber | string | 会员卡号 |
| name | string | 会员姓名 |
| phone | string | 手机号 |
| gender | number | 性别 |
| birthday | string | 生日 |
| totalPoints | number | 总积分 |
| totalConsumption | number | 累计消费金额 |
| status | number | 状态 |
| createTime | string | 创建时间 |
| updateTime | string | 更新时间 |
| lastConsumeTime | string | 最后消费时间 |

### 4.2 查询会员详情

- 请求方式：`GET`
- 接口路径：`/api/v1/members/{id}`
- 权限：`member:view`

### 4.3 新增会员

- 请求方式：`POST`
- 接口路径：`/api/v1/members`
- 权限：`member:add`

#### 请求体 `MemberUpsertDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| levelId | string | 否 | 会员等级 ID，不传时由后端分配默认等级 |
| cardNumber | string | 是 | 会员卡号 |
| name | string | 是 | 会员姓名 |
| phone | string | 是 | 手机号 |
| gender | number | 是 | 性别 |
| birthday | string | 否 | 生日 |
| status | number | 是 | 状态 |

### 4.4 修改会员

- 请求方式：`PUT`
- 接口路径：`/api/v1/members/{id}`
- 权限：`member:edit`

### 4.5 修改会员状态

- 请求方式：`PUT`
- 接口路径：`/api/v1/members/{id}/status`
- 权限：`member:edit`

#### 请求体 `StatusUpdateDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| status | number | 是 | 状态 |

### 4.6 导入会员 CSV

- 请求方式：`POST`
- 接口路径：`/api/v1/members/import`
- 权限：`member:import`
- Content-Type：`multipart/form-data`

#### 表单参数

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| file | file | 是 | CSV 文件 |

#### CSV 列顺序

```csv
cardNumber,name,phone,gender,birthday,status
```

## 5. 会员等级管理

### 5.1 查询等级列表

- 请求方式：`GET`
- 接口路径：`/api/v1/levels`
- 权限：`level:view`

#### 返回记录 `MemberLevelVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 等级 ID |
| name | string | 等级名称 |
| level | number | 等级值 |
| minPoints | number | 最低积分 |
| minConsumption | number | 最低消费金额 |
| discountRate | number | 折扣率 |
| pointRate | number | 积分倍率 |
| status | number | 状态 |
| createTime | string | 创建时间 |

### 5.2 查询等级详情

- 请求方式：`GET`
- 接口路径：`/api/v1/levels/{id}`
- 权限：`level:view`

### 5.3 新增等级

- 请求方式：`POST`
- 接口路径：`/api/v1/levels`
- 权限：`level:add`

#### 请求体 `MemberLevelUpsertDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | 是 | 等级名称 |
| level | number | 是 | 等级值 |
| minPoints | number | 是 | 最低积分 |
| minConsumption | number | 是 | 最低消费金额 |
| discountRate | number | 是 | 折扣率 |
| pointRate | number | 是 | 积分倍率 |
| status | number | 是 | 状态 |

### 5.4 修改等级

- 请求方式：`PUT`
- 接口路径：`/api/v1/levels/{id}`
- 权限：`level:edit`

### 5.5 删除等级

- 请求方式：`DELETE`
- 接口路径：`/api/v1/levels/{id}`
- 权限：`level:delete`

### 5.6 修改等级状态

- 请求方式：`PUT`
- 接口路径：`/api/v1/levels/{id}/status`
- 权限：`level:edit`

## 6. 消费记录管理

### 6.1 分页查询消费记录

- 请求方式：`GET`
- 接口路径：`/api/v1/consumptions`
- 权限：`consumption:view`

#### 查询参数 `ConsumptionQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| memberId | string | 否 | 会员 ID |
| storeId | string | 否 | 门店 ID |

#### 返回记录 `ConsumptionVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 消费记录 ID |
| memberId | string | 会员 ID |
| memberName | string | 会员姓名 |
| storeId | string | 门店 ID |
| storeName | string | 门店名称 |
| amount | number | 消费金额 |
| pointsEarned | number | 获得积分 |
| consumeTime | string | 消费时间 |
| createTime | string | 创建时间 |

### 6.2 查询消费记录详情

- 请求方式：`GET`
- 接口路径：`/api/v1/consumptions/{id}`
- 权限：`consumption:view`

### 6.3 新增消费记录

- 请求方式：`POST`
- 接口路径：`/api/v1/consumptions`
- 权限：`consumption:add`

#### 请求体 `ConsumptionCreateDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| memberId | string | 是 | 会员 ID |
| storeId | string | 是 | 门店 ID |
| amount | number | 是 | 消费金额，大于 0 |
| consumeTime | string | 否 | 消费时间，不传则取当前时间 |

#### 业务说明

新增消费记录后，后端会自动：

1. 更新会员累计消费金额
2. 计算积分
3. 写入积分明细
4. 触发会员等级升级判断

### 6.4 导入消费记录 CSV

- 请求方式：`POST`
- 接口路径：`/api/v1/consumptions/import`
- 权限：`consumption:import`
- Content-Type：`multipart/form-data`

#### CSV 列顺序

```csv
memberId,storeId,amount
```

## 7. 优惠券管理

### 7.1 分页查询优惠券

- 请求方式：`GET`
- 接口路径：`/api/v1/coupons`
- 权限：`coupon:view`

#### 查询参数 `CouponQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| keyword | string | 否 | 优惠券名称关键字 |
| status | number | 否 | 状态 |

#### 返回记录 `CouponVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 优惠券 ID |
| name | string | 名称 |
| type | number | 类型 |
| couponValue | number | 面值/优惠值 |
| useCondition | number | 使用门槛 |
| stock | number | 库存 |
| totalIssued | number | 已发放数量 |
| startTime | string | 开始时间 |
| endTime | string | 结束时间 |
| status | number | 状态 |
| createTime | string | 创建时间 |

### 7.2 查询优惠券详情

- 请求方式：`GET`
- 接口路径：`/api/v1/coupons/{id}`
- 权限：`coupon:view`

### 7.3 新增优惠券

- 请求方式：`POST`
- 接口路径：`/api/v1/coupons`
- 权限：`coupon:add`

#### 请求体 `CouponUpsertDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | 是 | 优惠券名称 |
| type | number | 是 | 优惠券类型 |
| couponValue | number | 是 | 优惠值 |
| useCondition | number | 否 | 使用门槛 |
| stock | number | 是 | 库存 |
| startTime | string | 是 | 生效时间 |
| endTime | string | 是 | 失效时间 |
| status | number | 是 | 状态 |

### 7.4 修改优惠券

- 请求方式：`PUT`
- 接口路径：`/api/v1/coupons/{id}`
- 权限：`coupon:edit`

### 7.5 删除优惠券

- 请求方式：`DELETE`
- 接口路径：`/api/v1/coupons/{id}`
- 权限：`coupon:delete`

### 7.6 修改优惠券状态

- 请求方式：`PUT`
- 接口路径：`/api/v1/coupons/{id}/status`
- 权限：`coupon:edit`

### 7.7 发放优惠券

- 请求方式：`POST`
- 接口路径：`/api/v1/coupons/{id}/issue`
- 权限：`coupon:issue`

#### 请求体 `CouponIssueDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| issueAll | boolean | 否 | 是否发给全部启用会员，默认 `false` |
| memberIds | string[] | 否 | 指定会员 ID 列表 |

## 8. 会员优惠券管理

### 8.1 分页查询会员优惠券

- 请求方式：`GET`
- 接口路径：`/api/v1/member-coupons`
- 权限：`memberCoupon:view`

#### 查询参数 `MemberCouponQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| memberId | string | 否 | 会员 ID |
| status | number | 否 | 状态 |

#### 返回记录 `MemberCouponVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 会员优惠券记录 ID |
| memberId | string | 会员 ID |
| memberName | string | 会员姓名 |
| couponId | string | 优惠券 ID |
| couponName | string | 优惠券名称 |
| code | string | 券码 |
| status | number | 状态 |
| receiveTime | string | 领取时间 |
| useTime | string | 使用时间 |
| expireTime | string | 过期时间 |

### 8.2 核销会员优惠券

- 请求方式：`PUT`
- 接口路径：`/api/v1/member-coupons/{id}/use`
- 权限：`memberCoupon:use`

## 9. 积分规则管理

### 9.1 分页查询积分规则

- 请求方式：`GET`
- 接口路径：`/api/v1/point-rules`
- 权限：`pointRule:view`

#### 查询参数 `PointRuleQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| ruleName | string | 否 | 规则名称 |
| status | number | 否 | 状态 |

#### 返回记录 `PointRuleVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 规则 ID |
| ruleName | string | 规则名称 |
| ruleType | number | 规则类型 |
| applicableLevelId | string | 适用等级 ID |
| pointsPerUnit | number | 积分倍率 |
| minAmount | number | 最低金额 |
| maxPoints | number | 单次上限积分 |
| status | number | 状态 |
| createTime | string | 创建时间 |

### 9.2 新增积分规则

- 请求方式：`POST`
- 接口路径：`/api/v1/point-rules`
- 权限：`pointRule:add`

#### 请求体 `PointRuleUpsertDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| ruleName | string | 是 | 规则名称 |
| ruleType | number | 是 | 规则类型 |
| applicableLevelId | string | 否 | 适用等级 ID |
| pointsPerUnit | number | 是 | 积分倍率 |
| minAmount | number | 否 | 最低金额 |
| maxPoints | number | 否 | 单次积分上限 |
| status | number | 是 | 状态 |

### 9.3 修改积分规则

- 请求方式：`PUT`
- 接口路径：`/api/v1/point-rules/{id}`
- 权限：`pointRule:edit`

### 9.4 删除积分规则

- 请求方式：`DELETE`
- 接口路径：`/api/v1/point-rules/{id}`
- 权限：`pointRule:delete`

### 9.5 修改积分规则状态

- 请求方式：`PUT`
- 接口路径：`/api/v1/point-rules/{id}/status`
- 权限：`pointRule:edit`

## 10. 积分明细管理

### 10.1 分页查询积分明细

- 请求方式：`GET`
- 接口路径：`/api/v1/point-details`
- 权限：`pointDetail:view`

#### 查询参数 `PointDetailQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| memberId | string | 否 | 会员 ID |
| type | number | 否 | 积分类型 |

#### 返回记录 `PointDetailVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 明细 ID |
| memberId | string | 会员 ID |
| type | number | 类型 |
| points | number | 变动积分 |
| balance | number | 当前余额 |
| source | string | 来源 |
| sourceId | string | 来源业务 ID |
| remark | string | 备注 |
| createTime | string | 创建时间 |

### 10.2 导入积分明细 CSV

- 请求方式：`POST`
- 接口路径：`/api/v1/point-details/import`
- 权限：`pointDetail:import`
- Content-Type：`multipart/form-data`

#### CSV 列顺序

```csv
memberId,type,points,balance,source,sourceId,remark
```

## 11. 分析模块

### 11.1 RFM 分析

- 请求方式：`GET`
- 接口路径：`/api/v1/analysis/rfm`
- 权限：`analysis:view`

#### 返回说明

返回 `Map<String, Object>`，主要字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| totalMembers | number | 会员总数 |
| segments | array | RFM 明细列表 |

### 11.2 生命周期分析

- 请求方式：`GET`
- 接口路径：`/api/v1/analysis/lifecycle`
- 权限：`analysis:view`

#### 查询参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| period | string | 否 | DAY | 统计周期，支持 `DAY` / `MONTH` |

### 11.3 客单价分布分析

- 请求方式：`GET`
- 接口路径：`/api/v1/analysis/behavior/order-amount`
- 权限：`analysis:view`

### 11.4 复购率分析

- 请求方式：`GET`
- 接口路径：`/api/v1/analysis/behavior/repurchase`
- 权限：`analysis:view`

### 11.5 消费时段分布分析

- 请求方式：`GET`
- 接口路径：`/api/v1/analysis/behavior/time-distribution`
- 权限：`analysis:view`

### 11.6 会员等级分布分析

- 请求方式：`GET`
- 接口路径：`/api/v1/analysis/behavior/level-count`
- 权限：`analysis:view`

#### 返回记录 `MemberLevelCountVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| levelId | string | 会员等级 ID |
| levelName | string | 会员等级名称 |
| count | number | 人数 |

### 11.7 每日消费趋势分析

- 请求方式：`GET`
- 接口路径：`/api/v1/analysis/behavior/daily-consume`
- 权限：`analysis:view`

#### 返回记录 `DailyConsumeVO`

| 字段 | 类型 | 说明 |
|---|---|---|
| consumeDate | string | 消费日期 |
| totalAmount | number | 当日消费总额 |

## 12. 门店管理

### 12.1 分页查询门店

- 请求方式：`GET`
- 接口路径：`/api/v1/stores`
- 权限：`store:view`

#### 查询参数 `StoreQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| keyword | string | 否 | 关键字，匹配名称/编码 |
| status | number | 否 | 状态 |

#### 返回记录 `StoreVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 门店 ID |
| name | string | 门店名称 |
| code | string | 门店编码 |
| address | string | 地址 |
| phone | string | 电话 |
| status | number | 状态 |
| createTime | string | 创建时间 |

### 12.2 查询门店详情

- 请求方式：`GET`
- 接口路径：`/api/v1/stores/{id}`
- 权限：`store:view`

### 12.3 新增门店

- 请求方式：`POST`
- 接口路径：`/api/v1/stores`
- 权限：`store:add`

#### 请求体 `StoreUpsertDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | 是 | 门店名称 |
| code | string | 是 | 门店编码 |
| address | string | 是 | 门店地址 |
| phone | string | 是 | 门店电话 |
| status | number | 是 | 状态 |

### 12.4 修改门店

- 请求方式：`PUT`
- 接口路径：`/api/v1/stores/{id}`
- 权限：`store:edit`

### 12.5 删除门店

- 请求方式：`DELETE`
- 接口路径：`/api/v1/stores/{id}`
- 权限：`store:delete`

### 12.6 修改门店状态

- 请求方式：`PUT`
- 接口路径：`/api/v1/stores/{id}/status`
- 权限：`store:edit`

## 13. 日志管理

### 13.1 分页查询操作日志

- 请求方式：`GET`
- 接口路径：`/api/v1/logs`
- 权限：`log:view`

#### 查询参数 `LogQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| operatorName | string | 否 | 操作人姓名 |
| module | string | 否 | 模块 |

#### 返回记录 `OperationLogVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 日志 ID |
| operatorId | string | 操作人 ID |
| operatorName | string | 操作人姓名 |
| module | string | 模块 |
| operation | string | 操作名称 |
| detail | string | 操作明细 |
| ip | string | IP 地址 |
| createTime | string | 创建时间 |

### 13.2 查询日志详情

- 请求方式：`GET`
- 接口路径：`/api/v1/logs/{id}`
- 权限：`log:view`

## 14. 管理员管理

### 14.1 分页查询管理员

- 请求方式：`GET`
- 接口路径：`/api/v1/admins`
- 权限：`admin:view`

#### 查询参数 `AdminQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| keyword | string | 否 | 关键字，匹配用户名/真实姓名/手机号 |
| status | number | 否 | 状态 |

#### 返回记录 `AdminVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 管理员 ID |
| username | string | 用户名 |
| realName | string | 真实姓名 |
| phone | string | 手机号 |
| status | number | 状态 |
| createTime | string | 创建时间 |

### 14.2 查询管理员已分配角色

- 请求方式：`GET`
- 接口路径：`/api/v1/admins/{id}/roles`
- 权限：`admin:view`

#### 返回字段 `AdminRoleIdsVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| roleIds | string[] | 已分配角色 ID 列表 |

### 14.3 新增管理员

- 请求方式：`POST`
- 接口路径：`/api/v1/admins`
- 权限：`admin:add`

#### 请求体 `AdminUpsertDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| username | string | 是 | 用户名 |
| password | string | 否 | 密码，不传则默认 `123456` |
| realName | string | 否 | 真实姓名 |
| phone | string | 否 | 手机号 |
| status | number | 是 | 状态 |

### 14.4 修改管理员

- 请求方式：`PUT`
- 接口路径：`/api/v1/admins/{id}`
- 权限：`admin:edit`

### 14.5 删除管理员

- 请求方式：`DELETE`
- 接口路径：`/api/v1/admins/{id}`
- 权限：`admin:delete`

### 14.6 修改管理员状态

- 请求方式：`PUT`
- 接口路径：`/api/v1/admins/{id}/status`
- 权限：`admin:edit`

### 14.7 分配管理员角色

- 请求方式：`PUT`
- 接口路径：`/api/v1/admins/{id}/roles`
- 权限：`admin:assign`

#### 请求体 `AdminRoleAssignDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| roleIds | string[] | 是 | 角色 ID 列表 |

## 15. 角色管理

### 15.1 分页查询角色

- 请求方式：`GET`
- 接口路径：`/api/v1/roles`
- 权限：`role:view`

#### 查询参数 `RoleQueryDto`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| keyword | string | 否 | 关键字，匹配角色名称/编码 |

#### 返回记录 `RoleVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 角色 ID |
| name | string | 角色名称 |
| code | string | 角色编码 |
| description | string | 角色描述 |
| createTime | string | 创建时间 |

### 15.2 查询角色已分配权限

- 请求方式：`GET`
- 接口路径：`/api/v1/roles/{id}/permissions`
- 权限：`role:view`

#### 返回字段 `RolePermissionIdsVo`

| 字段 | 类型 | 说明 |
|---|---|---|
| permissionIds | string[] | 已分配权限 ID 列表 |

### 15.3 新增角色

- 请求方式：`POST`
- 接口路径：`/api/v1/roles`
- 权限：`role:add`

#### 请求体 `RoleUpsertDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | 是 | 角色名称 |
| code | string | 是 | 角色编码 |
| description | string | 否 | 角色描述 |

### 15.4 修改角色

- 请求方式：`PUT`
- 接口路径：`/api/v1/roles/{id}`
- 权限：`role:edit`

### 15.5 删除角色

- 请求方式：`DELETE`
- 接口路径：`/api/v1/roles/{id}`
- 权限：`role:delete`

### 15.6 分配角色权限

- 请求方式：`PUT`
- 接口路径：`/api/v1/roles/{id}/permissions`
- 权限：`role:assign`

#### 请求体 `RolePermissionAssignDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| permissionIds | string[] | 是 | 权限 ID 列表 |

## 16. 权限管理

### 16.1 查询权限树

- 请求方式：`GET`
- 接口路径：`/api/v1/permissions/tree`
- 权限：`permission:view`

#### 返回记录 `PermissionTreeNode`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 权限 ID |
| name | string | 权限名称 |
| code | string | 权限编码 |
| type | number | 权限类型 |
| children | PermissionTreeNode[] | 子节点 |

### 16.2 新增权限

- 请求方式：`POST`
- 接口路径：`/api/v1/permissions`
- 权限：`permission:add`

#### 请求体 `PermissionUpsertDto`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | string | 是 | 权限名称 |
| code | string | 是 | 权限编码 |
| type | number | 是 | 权限类型 |
| parentId | string | 否 | 父级权限 ID |

### 16.3 修改权限

- 请求方式：`PUT`
- 接口路径：`/api/v1/permissions/{id}`
- 权限：`permission:edit`

### 16.4 删除权限

- 请求方式：`DELETE`
- 接口路径：`/api/v1/permissions/{id}`
- 权限：`permission:delete`

## 17. 当前系统权限编码

```text
member:view
member:add
member:edit
member:import
level:view
level:add
level:edit
level:delete
consumption:view
consumption:add
consumption:import
coupon:view
coupon:add
coupon:edit
coupon:delete
coupon:issue
memberCoupon:view
memberCoupon:use
pointRule:view
pointRule:add
pointRule:edit
pointRule:delete
pointDetail:view
pointDetail:import
analysis:view
store:view
store:add
store:edit
store:delete
log:view
admin:view
admin:add
admin:edit
admin:delete
admin:assign
role:view
role:add
role:edit
role:delete
role:assign
permission:view
permission:add
permission:edit
permission:delete
```

## 18. 文件导入说明

当前项目采用轻量化实现，导入接口均为 CSV 导入，不是 Excel 导入。

- 会员导入：`/api/v1/members/import`
- 消费导入：`/api/v1/consumptions/import`
- 积分明细导入：`/api/v1/point-details/import`

## 19. 常见错误示例

### 19.1 未登录

```json
{
  "code": 401,
  "message": "未授权访问",
  "data": null
}
```

### 19.2 无权限

```json
{
  "code": 403,
  "message": "无权限访问",
  "data": null
}
```

### 19.3 参数错误

```json
{
  "code": 400,
  "message": "请求参数错误",
  "data": null
}
```

### 19.4 业务异常

```json
{
  "code": 400,
  "message": "会员不存在",
  "data": null
}
```
