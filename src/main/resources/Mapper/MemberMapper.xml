<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bellego.mapper.MemberMapper">

    <select id="getLevelCount" resultType="com.bellego.domain.vo.MemberLevelCountVo">
        SELECT a.level_id AS levelId, b.name AS levelName, COUNT(*) AS count
        FROM member a
        LEFT JOIN member_level b ON a.level_id = b.id
        GROUP BY a.level_id
    </select>

</mapper>